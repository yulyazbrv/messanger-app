import { Flex, Title } from "@mantine/core";

const Home = () => {
  return (
    <Flex mt={50} align={"center"} justify={"center"} h={500}>
      <Title order={1}>Welcome!</Title>
    </Flex>
  );
};

export { Home };
